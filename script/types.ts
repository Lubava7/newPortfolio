export type ProjectType = {
  id: number;
  name: string;
  link: string;
  description: string;
  stack: string[];
  createdAt: string;
};

export type PagesType = {
  name: string;
  path: string;
};
